{
  "name": "@kompetenzhub/plugin-contracts",
  "version": "0.0.0",
  "private": true,
  "description": "KompetenzHub – gemeinsame Plugin-Verträge (Manifest, Capabilities, Lifecycle). Framework-frei.",
  "license": "AGPL-3.0-or-later",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "pretest": "tsc -p tsconfig.json",
    "test": "node --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
