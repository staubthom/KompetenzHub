name: Build and Push Monorepo Images

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Images
        run: |
          # Wir nutzen deinen exakten Build-Befehl
          docker compose -f docker-compose_hetzner.yaml --profile app build

      - name: Push Images
        run: |
          # Und pushen die getaggten Images hoch
          docker compose -f docker-compose_hetzner.yaml --profile app push